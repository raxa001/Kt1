# План API

## Книги
- GET /books — список всех книг
- GET /books/{id} — одна книга
- POST /books — добавить книгу
- DELETE /books/{id} — удалить книгу

## Выдача
- POST /loans — выдать книгу
- POST /loans/{id}/return — вернуть книгу
